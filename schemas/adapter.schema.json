{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/augentic/specify-cli/schemas/adapter.schema.json",
  "title": "Specify adapter manifest",
  "description": "Shared shape for source and target adapter manifests (`adapter.yaml`) per workflow §Adapter implementation shape. Carries the kebab-case `name`, semver `version`, the `axis` discriminator (`source` or `target`), the `briefs.<operation>` map of relative brief paths, and an optional singular `extension` declaration (RFC-48 D11). The set of operations is closed by the per-axis `briefs` constraints in `source.schema.json` and `target.schema.json` — `briefs.keys()` is the canonical operation iterator.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "version", "axis", "description", "briefs"],
  "properties": {
    "name": {
      "$ref": "#/$defs/kebabName",
      "description": "Adapter identifier in kebab-case. Must match the directory name under sources/ or targets/."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[0-9A-Za-z.-]+)?(\\+[0-9A-Za-z.-]+)?$",
      "description": "Semver adapter version (`x.y.z` with optional `-prerelease` / `+build`). Resolution keys on this string as the adapter's identity."
    },
    "axis": {
      "type": "string",
      "enum": ["source", "target"],
      "description": "Adapter role. `source` adapters survey leads and extract evidence; `target` adapters supply `shape`, `build`, and `merge` briefs."
    },
    "briefs": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "minLength": 1,
        "pattern": "^(?!/)(?![A-Za-z][A-Za-z0-9+.-]*://).+$"
      },
      "description": "Map from operation name to a relative path to the brief markdown file. Absolute paths and URIs are rejected. The per-axis schemas (`source.schema.json`, `target.schema.json`) close the legal key set."
    },
    "extension": {
      "$ref": "#/$defs/extensionDeclaration",
      "description": "Optional singular WASI extension declaration (RFC-48 D11). An adapter ships at most one binary built from the co-located `extension/` crate; the wasm rides the adapter's own semver identity (RFC-47) and content digest (D4), so a per-extension `version` / `source` / `sha256` is rejected."
    },
    "inputs": {
      "type": "array",
      "items": { "$ref": "#/$defs/buildInputDeclaration" },
      "description": "Optional adapter-declared build inputs. Target-only: the per-axis `target.schema.json` admits the key while `source.schema.json` rejects it; the shared shape carries the property so the closed key set admits it. Each entry is a path relative to the build request's `inputs.root`, flagged `required`."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable, single-sentence summary of the adapter's role. Required."
    },
    "platforms": {
      "type": "object",
      "description": "Optional declarative platforms capability. Target-only: the per-axis `target.schema.json` refines the shape with `$defs/platformsCapability`; `source.schema.json` rejects the key. The shared shape carries the property so the closed key set (from `additionalProperties: false`) admits it."
    },
    "execution": {
      "type": "string",
      "enum": ["agent", "tool"],
      "description": "Closed adapter execution mode (`agent` | `tool`). `required` is owned by the per-axis `source.schema.json` / `target.schema.json`; the shared shape carries the property so the closed key set admits it."
    }
  },
  "$defs": {
    "kebabName": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Kebab-case identifier: lowercase letters and digits grouped by single internal hyphens; no leading, trailing, or consecutive hyphens."
    },
    "buildInputDeclaration": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "required"],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(?!/)(?![A-Za-z][A-Za-z0-9+.-]*://).+$",
          "description": "Build input path relative to the build request's `inputs.root` (the slice tree). Absolute paths and URIs are rejected."
        },
        "required": {
          "type": "boolean",
          "description": "Whether `build` requires this input; a missing `required` path is a build-time abort."
        }
      }
    },
    "extensionDeclaration": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "$ref": "#/$defs/kebabName",
          "description": "Optional run handle for `specify extension run <name>`. Defaults to the adapter name when omitted."
        },
        "permissions": { "$ref": "#/$defs/extensionPermissions" }
      }
    },
    "extensionPermissions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "read": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Read-only preopen path templates (e.g. `$PROJECT_DIR/.specify`)."
        },
        "write": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Read-write preopen path templates."
        }
      },
      "description": "Structured WASI filesystem permissions in the `{read, write}` shape the runner already speaks."
    }
  }
}
