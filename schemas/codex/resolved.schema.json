{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/augentic/specify-cli/schemas/codex/resolved.schema.json",
  "title": "Specify resolved codex export",
  "description": "Wire shape emitted by `specrun codex export --format json` per RFC-28 §Resolved codex export. The export is the read-only resolved view of shared, source-adapter, and target-adapter codex rules for a project, target adapter, slice, or artifact path. Frontmatter `snake_case` keys (`review_mode`, `deterministic_hints`, `deprecated.replaced_by`) are emitted as kebab-case at every nesting level (`review-mode`, `deterministic-hints`, `deprecated.replaced-by`). The schema is the authoritative source for exported field names; producers MUST validate output against it.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "target-adapter",
    "source-adapters",
    "rules"
  ],
  "properties": {
    "version": {
      "type": "integer",
      "const": 1,
      "description": "Envelope version. v1 pins this to 1; widening the export contract requires a v2 envelope."
    },
    "target-adapter": {
      "$ref": "#/$defs/adapterRef",
      "description": "Resolved target-adapter name, optionally versioned as `<name>@v<major>`."
    },
    "source-adapters": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/adapterRef"
      },
      "description": "Source-adapter names bound to the export context. May be empty when the resolver is invoked without source bindings."
    },
    "rules": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/rule"
      },
      "description": "Ordered rule list. Producer ordering is `(non-deprecated, severity, origin, rule-id)` per RFC-28 §Resolved codex export."
    }
  },
  "$defs": {
    "ruleId": {
      "type": "string",
      "pattern": "^(UNI|SRC|FRAME|RUST|IFACE|SEC|OMNIA|VECTIS|ORG)-[0-9]{3}$",
      "description": "Closed v1 rule-id namespace (`UNI`, `SRC`, `FRAME`, `RUST`, `IFACE`, `SEC`, `OMNIA`, `VECTIS`, `ORG`) with three-digit numeric suffixes. Mirrors `codex-rule.schema.json#/$defs/ruleId`."
    },
    "adapterRef": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*(?:@v[1-9][0-9]*)?$",
      "description": "Kebab-case adapter token, optionally suffixed with `@v<major>`. Mirrors `codex-rule.schema.json#/$defs/adapterRef`."
    },
    "kebabToken": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "languageToken": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9+#.-]*$"
    },
    "relativePathPattern": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?![A-Za-z][A-Za-z0-9+.-]*://)(?!.*(?:^|/)\\.\\.(?:/|$)).+$",
      "description": "Project-relative path or glob. Absolute paths, URI strings, and `..` segments are rejected. Mirrors `codex-rule.schema.json#/$defs/relativePathPattern`."
    },
    "severity": {
      "type": "string",
      "enum": [
        "critical",
        "important",
        "suggestion",
        "optional"
      ],
      "description": "Closed severity enum, ordered for sort stability (`critical < important < suggestion < optional`). Producer-side severity-aware comparators use this sequence; JSON Schema does not enforce ordering."
    },
    "origin": {
      "type": "string",
      "enum": [
        "shared",
        "source",
        "target",
        "organization"
      ],
      "description": "Resolver origin tier. `shared` rules come from `adapters/shared/codex/`; `source` from a source-adapter overlay; `target` from a target-adapter overlay; `organization` is reserved for project-local overlays."
    },
    "pathRoot": {
      "type": "string",
      "enum": [
        "codex-root",
        "project-dir"
      ],
      "description": "Anchor for the rule `path`. Shared rules and codex-root fallback overlays anchor at `codex-root`; project-local and cached overlays anchor at `project-dir`."
    },
    "reviewMode": {
      "type": "string",
      "enum": [
        "deterministic",
        "model-assisted",
        "hybrid"
      ],
      "description": "How the rule is expected to be reviewed. The review runner decides execution; this field only classifies the rule."
    },
    "hintKind": {
      "type": "string",
      "enum": [
        "path-pattern",
        "regex",
        "schema",
        "tool",
        "unique",
        "reference-resolves",
        "set-coverage",
        "cardinality",
        "constant-eq",
        "set-eq",
        "content-digest-eq",
        "namespace-owner"
      ],
      "description": "Closed v1 deterministic-hint kind enum. Reserved kinds (`unique`, `reference-resolves`, `set-coverage`, `cardinality`, `constant-eq`, `set-eq`, `content-digest-eq`, `namespace-owner`) are shape-validated only; RFC-32 owns execution semantics."
    },
    "deterministicHint": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "value"
      ],
      "properties": {
        "kind": {
          "$ref": "#/$defs/hintKind"
        },
        "value": {
          "type": "string",
          "minLength": 1,
          "description": "Hint payload, interpreted by a future validator or review tool."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "Optional human explanation for the hint."
        }
      }
    },
    "reference": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "label"
      ],
      "anyOf": [
        {
          "required": [
            "url"
          ]
        },
        {
          "required": [
            "path"
          ]
        }
      ],
      "properties": {
        "label": {
          "type": "string",
          "minLength": 1,
          "description": "Short display label for the reference."
        },
        "url": {
          "type": "string",
          "pattern": "^https?://.+$",
          "description": "HTTP(S) reference URL."
        },
        "path": {
          "$ref": "#/$defs/relativePathPattern",
          "description": "Repository-relative reference path. Resolves against the rule's `path-root`."
        }
      }
    },
    "applicability": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": false,
      "description": "Inclusive narrowing filter. All populated dimensions must match (AND semantics).",
      "properties": {
        "adapters": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/adapterRef"
          }
        },
        "languages": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/languageToken"
          }
        },
        "artifacts": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/kebabToken"
          }
        },
        "paths": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/relativePathPattern"
          }
        }
      }
    },
    "deprecatedInfo": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "reason"
      ],
      "description": "Deprecation metadata. Frontmatter `replaced_by` exports as wire-key `replaced-by`.",
      "properties": {
        "reason": {
          "type": "string",
          "minLength": 1
        },
        "replaced-by": {
          "$ref": "#/$defs/ruleId"
        }
      }
    },
    "rule": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "rule-id",
        "title",
        "severity",
        "trigger",
        "origin",
        "path-root",
        "path",
        "body"
      ],
      "properties": {
        "rule-id": {
          "$ref": "#/$defs/ruleId"
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "maxLength": 120
        },
        "severity": {
          "$ref": "#/$defs/severity"
        },
        "trigger": {
          "type": "string",
          "minLength": 1,
          "maxLength": 500
        },
        "review-mode": {
          "$ref": "#/$defs/reviewMode"
        },
        "applicability": {
          "$ref": "#/$defs/applicability"
        },
        "deterministic-hints": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/deterministicHint"
          }
        },
        "references": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/reference"
          }
        },
        "origin": {
          "$ref": "#/$defs/origin"
        },
        "path-root": {
          "$ref": "#/$defs/pathRoot"
        },
        "path": {
          "$ref": "#/$defs/relativePathPattern",
          "description": "Path to the codex rule markdown file, relative to `path-root`."
        },
        "body": {
          "type": "string",
          "description": "Verbatim markdown body (everything after the closing `---` frontmatter delimiter), including section headings such as `## Rule`."
        },
        "deprecated": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/deprecatedInfo"
            }
          ],
          "description": "Deprecation metadata, or `null` when the rule is active. Absence and explicit `null` are equivalent."
        }
      }
    }
  }
}
