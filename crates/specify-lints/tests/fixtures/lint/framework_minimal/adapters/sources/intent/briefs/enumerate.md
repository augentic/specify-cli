# intent.enumerate

Minimal brief fixture for the framework indexer.

## Inputs

- `Source` — the operator-bound `intent` source.

## Output contract

One candidate block under `## Candidate inventory`.
