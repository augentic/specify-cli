[package]
name = "specify-validate"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Baseline-contract validation primitives shared by the host CLI (`specify-domain`) and the standalone `wasi-tools/contract` carve-out"
categories = ["development-tools"]
keywords = ["specify", "validate", "openapi", "asyncapi", "contracts"]
readme = "../../README.md"

[lints]
workspace = true

[dependencies]
semver = { workspace = true }
serde = { workspace = true }
serde-saphyr = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
