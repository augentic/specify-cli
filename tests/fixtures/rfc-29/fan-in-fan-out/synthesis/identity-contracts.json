{
  "version": 1,
  "kind": "response",
  "slice": "identity-contracts",
  "model": {
    "requirements": [
      {
        "title": "Identity API contract",
        "unit": "identity",
        "claims": [
          { "source": "docs", "id": "identity-api.contract", "kind": "requirement" }
        ],
        "statement": "The identity API exposes authentication and account-access endpoints.\n\n#### Scenario: Contract is published\n\n- GIVEN the identity domain\n- WHEN the contract is generated\n- THEN clients can authenticate against it"
      }
    ],
    "tasks": [
      { "id": "TASK-001", "text": "Author the identity API contract.", "satisfies": ["REQ-001"] }
    ]
  },
  "artifacts": {
    "proposal": "# Identity contracts\nDefine the versioned identity API contract.\n",
    "design": "# Design\nContract surface for authentication and account access.\n",
    "tasks": "# Tasks\n- [ ] TASK-001 Author the identity API contract.\n",
    "specs": [
      { "unit": "identity", "content": "## Identity API contract\nThe identity API exposes authentication and account-access endpoints.\n" }
    ]
  }
}
