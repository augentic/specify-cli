---
id: merge
description: Merge brief — describes baseline merge for Omnia slices.
---

The Omnia target's `merge` brief describes the deterministic merge
protocol that folds a slice's deltas into the project baseline. The
heavy lifting is done by `specify slice merge`; this brief documents
the per-target tweaks (terminology, contract handling) the merge step
honours.
