[package]
name = "specify-framework-wire"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared stdout envelope (DiagnosticReport DTOs, report printer, sentinel-arg helpers) for the framework-authoring WASI tools"
readme = "../../README.md"
categories = ["development-tools"]
keywords = ["specify", "lint", "framework", "wasi"]

[lib]
name = "specify_framework_wire"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true

[lints]
workspace = true
