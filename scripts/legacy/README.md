# Legacy scripts (reference-only)

These scripts are kept as historical reference for the deterministic
operations now provided by the `specify` CLI. They are NOT invoked by
any skill or workflow. Do not modify them — bug fixes belong in the
Rust crates under `crates/`.

| File | Replaced by | First absent in |
|---|---|---|
| `merge-specs.py` | `specify merge` (RFC-1 Change J) | RFC-1 Change K |
