# Baselines for scripts/standards-check.sh.
#
# Each line is `KEY=COUNT`. Reductions are encouraged; raising a number
# requires a justification in the PR description. Lines starting with `#`
# and blank lines are ignored.
#
# AGENTS.md#coding-standards is the source of truth for what each rule
# enforces; this file just stores the high-water marks.

# Comments — RFC numbers / migration prose in code outside DECISIONS.md and
# rfcs/. Reduced phase-by-phase by phase-2-comments.
rfc-numbers-in-code=204

# DTOs — `#[derive(...Serialize...)]` inside a fn body.
inline-dtos=0

# Errors — free-form Error::Variant(String) catch-alls. Phase 1 renames
# SchemaResolution -> CapabilityResolution (preserves the count); phase-5
# reduces by introducing structured Error::Argument and Error::Merge.
free-form-error-strings=280

# Format dispatch — hand-rolled `match ctx.format { Json => ... }`. Phase 3
# replaces these with `emit(format, &Body)` via the Render trait.
format-match-dispatch=70
